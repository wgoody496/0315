# 遠端不存在 電腦沒設定 完全空白的狀態

到github開一個帳號, 開一個倉庫
接下一段

# 每個禮拜來上課  新電腦  未設定

```
git config --global user.name "FilitovDemo"
git config --global user.email "filitov@twcat.net"
```

切換到資料夾, 順便清空

```
git clone https:github.com/FilitovDemo/test2018.git .
```

# 自己的隨身碟
(知道怎麼修改伺服器的網頁根目錄路徑)

第一次...切換到資料夾

```
git config user.name "FilitovDemo"
git config user.email "filitov@twcat.net"
git clone https:github.com/FilitovDemo/test2018.git .
```

以後

```
git pull
```

# 自己的電腦, 已經設定過的

```
git pull
```

# 完工之後 (已經設定過的)

```
git add 檔案們
git commit -m "做了甚麼"
```
可能很多次 add commit

```
git push
```
